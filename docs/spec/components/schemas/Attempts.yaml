allOf:
  - $ref: "#/components/schemas/AttemptsKey"
  - type: object
    required:
      - attributes
    properties:
      attributes:
        type: object
        required:
          - attempts_left
        properties:
          attempts_left:
            type: integer
            format: int64
            description: remained number of attempts to retrieve user's public key
            example: 7
